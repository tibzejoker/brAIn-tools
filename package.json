{
  "name": "brAIn-tools",
  "private": true,
  "license": "MIT",
  "description": "brAIn tool nodes — shell, python eval, HTTP bridge.",
  "scripts": { "build": "pnpm -r build", "clean": "pnpm -r exec rm -rf dist" },
  "devDependencies": { "@types/node": "^20.11.0", "typescript": "^5.4.0" }
}
